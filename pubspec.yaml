name: automatic_watering_mobile
description: Mobile app for an automatic watering system.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  flutter_reactive_ble: ^5.4.0
  flutter_secure_storage: ^9.2.4
  network_info_plus: ^7.0.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.3
  wifi_scan: ^0.4.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
