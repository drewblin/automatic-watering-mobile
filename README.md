# Automatic Watering Mobile

Flutter mobile application for an automatic watering system.

## Setup

Install Flutter, then run:

```sh
flutter pub get
flutter create --platforms=android,ios .
flutter analyze
flutter test
```

Use `flutter run` to start the app on a connected device or emulator.
